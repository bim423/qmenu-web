const API_ROOT = "http://localhost:8080/"
const WEB_ROOT = "http://localhost:63343/"

const API = {
    MENU : API_ROOT + "menu",
    PRODUCT : API_ROOT + "product",
    SUBMENU : API_ROOT + "submenu",
    DESK : API_ROOT + "desk",
    ORDER : API_ROOT + "order"
}